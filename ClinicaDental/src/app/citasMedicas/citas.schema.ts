/*import * as Joi from 'joi';

export const CITA_SCHEMA = Joi
  .object()
  .keys({
    fechaCita: Joi
      .string()
      .alphanum()
      .min(3)
      .max(30)
      .required(),

    horaCita: Joi
      .string()
      .alphanum()
      .min(3)
      .max(30)
      .required(),
  });
  */
